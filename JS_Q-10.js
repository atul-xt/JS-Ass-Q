// Qs10. Write a JavaScript program to test whether the character at the given (character)
// index is lower case.

let str = "Atul Sadiwal";
let index = 1;

if ( str[index] === str[index].toLowerCase(str))
{
    console.log("Character is Lower Case");
}
else
{
    console.log("Character isn't Lower Case");
}